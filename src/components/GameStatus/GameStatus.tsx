import { Container, StatusIndicator, Id } from './GameStatus.styled';

export default function GameStatus() {

    return (
        <Container>
            {/* <StatusIndicator status={session.connected} />
            <Id>Game Id: {session.gameId}</Id> */}
        </Container>
    );
}